# tinotax-ledger/src

Source modules for reviewed ledger construction.

## Module Map

- `build.rs` builds reviewed ledger events.
- `csv_export.rs` writes reviewable ledger CSVs.
- `lib.rs` exposes the crate API.
