# tinotax-core/src

Source modules for shared domain records.

## Invariants

- Keep this crate free of filesystem and network IO.
- Use decimal/string-preserving representations for money and quantities.
- Stable IDs and enum strings are part of the cross-crate contract.
